- daemon health command + way to manage the daemon
- daemon logging
- error sentinels -> user-friendly error messages
- add url to problem proto so that if the right tab isn't open, the extension
  can open it
- add logging for cli control flow
- add auto-update
- add submission tracking in extension
- write jetbrains platform plugin (for clion)
  - should be kind of like the extension in that the daemon is the hub, the plugin just does
    the hub's bidding (in fact, use the hub struct)
  - consider adding vscode support as well, since it's not too much work anyway
- github workflows; making everything easy to install
- make cli stuff prettier